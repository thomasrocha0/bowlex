/**
 * Isolated from AddGamesModal so a future settings page can swap this for a
 * persisted user preference without touching the component.
 */
export const DEFAULT_GAME_COUNT = 1;
export const MAX_GAMES_PER_SERIES = 10;
