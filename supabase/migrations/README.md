SQL migrations for the Postgres schema (tables, RLS policies, views, and the
`get_friend_stats` security-definer function described in CLAUDE.md). Generate
new migrations with the Supabase CLI:

```
supabase migration new <name>
```
